# HTTPS-redirect middleware
http:
  middlewares:
    redirect:
      redirectScheme:
        scheme: https
    #auth:
    #  basicAuth:
    #    users:
    #      - "admin:$$apr1$$6oTRFVx4$$K3GCcv0TDAMjD.ptGG.Li."